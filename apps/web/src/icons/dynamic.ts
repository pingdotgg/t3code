/* eslint-disable no-restricted-imports -- this seam owns Lucide's dynamic icon loader. */
export { DynamicIcon, iconNames } from "lucide-react/dynamic";
export type { IconName } from "lucide-react/dynamic";
