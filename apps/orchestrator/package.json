{
  "name": "@t3tools/orchestrator",
  "version": "0.0.15",
  "private": true,
  "type": "module",
  "scripts": {
    "convex:disable-ai-files": "bunx convex ai-files disable",
    "dev": "bun run convex:disable-ai-files && bunx convex dev",
    "deploy": "bun run convex:disable-ai-files && bunx convex deploy",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@chat-adapter/linear": "^4.25.0",
    "@convex-dev/agent": "^0.6.1",
    "@t3tools/contracts": "workspace:*",
    "chat": "^4.25.0",
    "convex": "^1.35.1",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
