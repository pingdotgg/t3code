import Foundation

enum MobileDesign {
    static let connectionSummary = "Direct / Tailscale"
    static let placeholderMessage = "Connect to a T3 Code server to sync real projects and threads."
    static let spacing: Double = 12
    static let cornerRadius: Double = 12
}
