export {
  DEFAULT_MODEL,
  MODEL_OPTIONS,
  normalizeModelSlug,
  resolveModelSlug,
} from "@acme/contracts";

export const REASONING_OPTIONS = ["xhigh", "high", "medium", "low"] as const;
export const DEFAULT_REASONING = "high";
