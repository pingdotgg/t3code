# Superseded: Auldric hard-fork launch plan

This path is retained only so old links fail safely. The plan from the legacy
`AuldricAI/Auldric` repository at `cf6400e77dfaf9569f1ce6eaca4421deb0b2bf23` is superseded.

Do not use it to implement a downstream runtime, custom platform infrastructure, broad namespace
replacement, or selective T3 intake. T3 is authoritative for Dev and shared infrastructure. The
only active Auldric product scope is the isolated Marketing/Strategy domain described by the
[canonical spine](./auldric-system/README.md).

Historical source paths and the reasons they are unsafe are recorded in the
[archive index](./_archive/auldric-runtime/README.md). Issue #14 separately owns donor inventory and
is the only route by which a bounded Marketing requirement may be approved for rebuild.

**Decision:** retired as an implementation plan.

**Next action:** follow the canonical Marketing issue dependency graph.

**Parked until:** every legacy capability unless #14 classifies it and an executable issue owns it.
