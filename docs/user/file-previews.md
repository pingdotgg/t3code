# Preview files

Open a file to view its contents beside the file explorer. Drag the divider between the file and
the explorer to change the explorer width. Double-click the divider to restore the default width.

T3 Code saves the width in the current browser. On narrow windows, the explorer stays visible and
the file content uses the remaining space.
