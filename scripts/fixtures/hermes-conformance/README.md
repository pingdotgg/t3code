# Hermes conformance fixtures

Generated sanitized JSONL may be reviewed and copied here only after a run against the exact
official pinned revision. The sanitizer is allowlist-based; reviewers must still inspect the output
before copying it. Never commit raw capture, access tokens (including numeric credentials), profile
contents, prompts, tool arguments/output, errors, URLs, attachment bytes, filenames, or host paths.

No golden capture is checked in yet. The local Hermes 0.19.0 installation and synthetic/example
frames are not substitutes for pinned live evidence. Use
`docs/integrations/hermes-conformance.md` and retain blocked/indeterminate classifications.
